- Dentro da pasta do projeto:

cd nome-da-pasta
cd nome-da-pasta/nome-da-outra-pasta
exit
dir
cd ../
django-admin startproject core
move core nomedoprojeto/
cd scripts
active.bat
manage.py migrate
python manage.py runserver
python manage.py startapp core
pip install
python manage.py shell
python manage.py sqlall nomedapasta

=======================================

- Dentro do python:

import django
django.get_version()
exit()
crtl+pause/break
from nomedoarquivo import *(Significa tudo)

=======================================

- Dentro do python shell:

from simplemooc.courses.models import Course
course = Course()
course.name = "Python na Web com Django"
course.slug = "django"
from datetime import date
course.start_date = date.today()
course.save()
course.id
course.pk
course.delete()
django = Course(name='Python com Django', slug='django')
python_dev = Course(name='Python para Devs', slug='python-dev')
courses = Course.objects.all()
for course in courses:
	print(course.name)
from simplemooc.courses.models import Course
Course.objects

=======================================

localhost:8000